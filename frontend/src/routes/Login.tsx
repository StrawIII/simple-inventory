import { LoginForm } from "../components/LoginForm";

export const Login = () => {
  return (
    <div className="flex h-screen w-full min-w-80 items-center justify-center">
      <LoginForm />
    </div>
  );
};
